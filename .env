# This file contains the ENV vars necessary to run the app locally using the
# 'heroku local' command.
# IMPORTANT: DO NOT store sensitive data here.
# Use 'heroku config:set' command to define your environment variables in your
# production or staging environment. For instance, you can run the following
# command to set your graphql endpoint for an app that is deployed to heroku:
#
# heroku config:set REACT_APP_GRAPHQL_URI=https://YOUR_APP_NAME.herokuapp.com/graphql
#
# Then you will need to force heroku to rebuild the app so that the recently set
# env var can be embedded during the next build of your Create React App (CRA).
# Remember, CRA produces a static HTML/CSS/JS bundle, so it can’t read env vars
# at runtime! You can do that by pushing an empty commit:
#
# git commit --allow-empty -m "This commit does not change any code"
# git push heroku master
#
# For more information see this: https://github.com/facebookincubator/create-react-app/blob/master/packages/react-scripts/template/README.md#adding-custom-environment-variables



# This file contains all ENV vars necessary to run the app LOCALLY for both
# development and production modes.
#
# IMPORTANT: DO NOT store sensitive data here, this file may be committed to
# version control!
#
# In case of production or staging deployments (apps hosted on heroku), use the
# 'heroku config:set' command (heroku command line tool) to set your environment
# variables. For instance, let's suppose we want to set MONGO_URL for our
# production deployment:
# 1. Install the heroku command line tool: https://devcenter.heroku.com/articles/heroku-cli
# 2. Open a new terminal and type: 'heroku login'. Enter your credentials.
# 3. Set your MONGO_URL env var:
# heroku config:set MONGO_URL=mongodb://<dbuser>:<dbpassword>@ds129459.mlab.com:<port>/<dbname>


REACT_APP_GRAPHQL_URI=http://localhost:3000/graphql


# Example of MONGO_URL env var for local apps connected local MongoDB instance:
#MONGO_URL=mongodb://localhost:27017/crae-apollo

JWT_PRIVATE_KEY=techtown # just a random string (pick any) to generate the json token
SMTP_HOST=xxx # if you use Mailgun --> smtp.mailgun.org
SMTP_USERNAME=xxx # if you use Mailgun --> postmaster@sandboxXXXX.mailgun.org
SMTP_PASSWORD=xxx
SMTP_PORT=587 # if you use Mailgun --> 587
NODE_ENV=development
PORT=4000

# Example of MONGO_URL env var for local apps connected to mLab MongoDB instance:
MONGO_URL=mongodb://web:Time2go@ds149412.mlab.com:49412/heroku_lrtnbx3s
